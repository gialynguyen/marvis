{
  "name": "marvis-monorepo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.30.3",
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "test": "turbo test",
    "test:run": "turbo test:run",
    "typecheck": "turbo typecheck",
    "lint": "turbo lint",
    "lint:unsafe": "turbo lint:unsafe",
    "format": "turbo format"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.8.0",
    "turbo": "^2.0.0",
    "typescript": "^5.4.0",
    "@marvis/cli": "workspace:*"
  }
}
